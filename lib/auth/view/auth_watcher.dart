import 'package:flutter/material.dart';

class AuthWatcher extends StatelessWidget {
  const AuthWatcher({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Auth watcher");
  }
}