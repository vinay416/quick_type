import 'package:flutter/material.dart';

class NewTakeView extends StatelessWidget {
  const NewTakeView({super.key});

  static String get routeName => 'new';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
